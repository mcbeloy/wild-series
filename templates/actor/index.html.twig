{% extends 'base.html.twig' %}


{% block body %}

<div>
    <h1>Hello les acteurs! </h1>
    <ul>
        {% for actor in actors %}
            <li><a href="{{ path("actor_show", { id: actor.id }) }}">{{ actor.name }}></a></li>
        {% else %}
            <li>Pas d'acteur pour cette série.</li>
        {% endfor %}
    </ul>
</div>

{% endblock %}